# Past Modeling Experiments

The Data Department has implemented and tested numerous improvements to the model-res-avm repository across modeling, infrastructure, reporting, and feature engineering. Not all tests and experiments have been incorporated into the final model. This document serves as a historical record of what has been explored, what was adopted, and what was set aside.

Some entries originate from the [Enterprise Intelligence repository](https://github.com/ccao-data/enterprise-intelligence). This repository is private, so a summary is provided here for public reference.

## Themes

| Theme | Description |
| --- | --- |
| 🔵 Model Architecture | Experiments about *what* the model is — which algorithm or engine to use, whether to segment by geography or property type, and how to structure the model. |
| 🟢 Training Process | Experiments about *how* the model is trained — hyperparameter tuning strategies, cross-validation methodology, preprocessing steps, and training evaluation. |
| 🟣 Model Features | Experiments testing new or modified input features and predictors. |
| 🔴 Comps | Experiments related to the comparable sales (comps) pipeline and tree-weighting algorithm. |

| Question / Goal | Issue | Findings / Output | Theme | Date Closed |
| --- | --- | --- | --- | --- |
| Should `bonsai` (the official Tidymodels/parsnip shim for LightGBM) replace `Lightsnip`? | [#16](https://github.com/ccao-data/model-res-avm/issues/16) | `{bonsai}` is promising but not ready for adoption — it lacks advanced LightGBM hyperparameters and early stopping. Sticking with `Lightsnip`. | 🔵 Model Architecture | 2023-12-05 |
| Test a City of Chicago–only model (using expanded city-specific data and only City triad sales) and compare performance to city-only ratios from county-wide models. | [#28](https://github.com/ccao-data/model-res-avm/issues/28) | Tested two configurations (Chicago-only sales; Chicago-only sales + large cross-validation). No change in performance in either case. | 🔵 Model Architecture | 2024-01-18 |
| Prior CCAO residential models have used a single model that encompasses both single- and multi-family properties. We may achieve better accuracy for multi-family properties if we instead separate them out in a separate model. | [#29](https://github.com/ccao-data/model-res-avm/issues/29) | The result is essentially nil — splitting the models has no significant impact on overall performance statistics. This is somewhat expected, as the model already has access to plenty of information to differentiate between single- and multi-family properties. | 🔵 Model Architecture | 2023-12-05 |
| The `grid` argument of `tune_bayes()` can use a result from `tune_grid()`. We should use this to "seed" `tune_bayes()` with a manual or space-filling grid. | [#36](https://github.com/ccao-data/model-res-avm/issues/36) | The technique ended up being far slower than Bayesian optimization with early stopping. | 🟢 Training Process | 2023-12-25 |
| Per recommendation from folks at posit::conf, test out the `Cubist` engine from the `rules` R package to see how it performs on our data. | [#37](https://github.com/ccao-data/model-res-avm/issues/37) | `Cubist` outperforms a linear model but not the current main model. Major drawbacks: single-threaded (very slow to train) and high memory usage (~250 GB), making a full grid search infeasible. | 🔵 Model Architecture | 2023-12-25 |
| We may be able to cross-validate significantly faster using racing-based `tune` methods, which are available in the `finetune` R package. Test cross-validation techniques: standard grid search (with sub-model trick), tune racing, and simulated annealing. | [#38](https://github.com/ccao-data/model-res-avm/issues/38) | All techniques were slower than Bayesian optimization with early stopping. Sub-model fitting works but requires fitting on the maximum number of iterations. Grid racing is faster than standard grid search but has the same limitation. Simulated annealing is too slow. Bayesian optimization remains the best approach. | 🟢 Training Process | 2023-12-25 |
| Revisit ensemble/stacked modeling using the `stacks` package (prior attempt with Tidymodels was inconclusive due to immaturity of the tooling at the time). | [#46](https://github.com/ccao-data/model-res-avm/issues/46) | Abandoned without testing. Though we did not generate hard performance data, we decided that the added complexity of a stacked model was not worth it compared to a standard boosted tree model in principle. | 🔵 Model Architecture | 2024-12-06 |
| Test categorical embeddings (via `embed.tidymodels.org`) as an alternative to LightGBM's native categorical handling. | [#49](https://github.com/ccao-data/model-res-avm/issues/49) | Embeddings performed worse than LightGBM's native handling. The `embed` package also has heavy dependencies. Embeddings removed entirely (including from the linear model). | 🟢 Training Process | 2024-01-27 |
| Test a homeowner exemption model feature as a proxy for owner-occupied status. | [#50](https://github.com/ccao-data/model-res-avm/issues/50) | We found the feature to have predictive power, so we implemented it in https://github.com/ccao-data/model-res-avm/pull/179. However, we ended up removing it at the request of Valuations in https://github.com/ccao-data/model-res-avm/pull/339. | 🟣 Model Features | 2024-01-20 |
| Test `Tidymodels` "1 standard deviation" rule selector to see if it improves generalization to a held-out validation set. | [#134](https://github.com/ccao-data/model-res-avm/issues/134) | The "best" model consistently generalizes better than the 1 SD selection. The 1 SD selector tends to prefer many shallow trees (3,000 iterations, <100 `num_leaves`) over fewer but deeper trees (1,500 iterations, <250 `num_leaves`). | 🟢 Training Process | 2024-02-07 |
| Add a price-strata or lagged-price feature to give the model a starting price point, enabling it to segment the market by value tier. | [#160](https://github.com/ccao-data/model-res-avm/issues/160) | Two problems identified: sold and unsold properties are treated differently due to the 3-year assessment cycle, and the lag price feature effectively leaks the outcome into the assessment set as a predictor. Feature scrapped. | 🟣 Model Features | 2024-02-03 |
| Update cross-validation: revert to rolling-origin splitting, make it non-cumulative, and store per-fold predictions and residuals. | [#161](https://github.com/ccao-data/model-res-avm/issues/161) | New sliding-window CV function added with: switchable random (v-fold) vs. recency-based (sliding window) splitting via `validation_type`; non-cumulative folds; and overlapping fold support. | 🟢 Training Process | 2024-01-16 |
| Replace LightGBM's native missingness handling with more targeted imputation using `recipes::step_impute_*` functions. | [#162](https://github.com/ccao-data/model-res-avm/issues/162) | No meaningful performance difference. Missingness is limited and LightGBM handles it well natively. Advanced strategies (bagging, KNN) were too slow to test even on large AWS instances. | 🟢 Training Process | 2024-01-29 |
| Test log-transforming the outcome (sale price) and skewed input features (income, building sqft, etc.) to improve model performance, particularly at the low end. | [#163](https://github.com/ccao-data/model-res-avm/issues/163) | Results are inconclusive. Slightly improves COD and low-end accuracy but worsens overall RMSE and high-end accuracy. See commit [`8a7ae37`](https://github.com/ccao-data/model-res-avm/commit/8a7ae37888c7b3396184eab320e144f16c857fda) and runs `2024-01-24-sagacious-liz` / `2023-12-26-kind-caroline`. | 🟢 Training Process | 2024-01-23 |
| Test smaller geographic units as location features: census tracts + PUMA, census block groups + PUMA, and Sidwell ID broken out by block. | [#166](https://github.com/ccao-data/model-res-avm/issues/166) | No meaningful improvements across all tested combinations. Census tracts as numeric yielded a minor improvement (<1%). Sidwell ID by block performed worse than standard location predictors. | 🟣 Model Features | 2024-01-17 |
| The comps pipeline produces unexpected results for high-value cards, likely because tree weights are computed globally (based on training-set RMSE) rather than per observation. | [#193](https://github.com/ccao-data/model-res-avm/issues/193) | Updated the comps pipeline (`python/comps.py`) to use per-observation tree weights instead of global weights. | 🔴 Comps | 2024-02-03 |
| Explore strategies for modeling multi-card properties, which are excluded from training because sale price cannot be attributed per card. | [#228](https://github.com/ccao-data/model-res-avm/issues/228) | Two best strategies identified: `single card summing sqft` and `multi-card sqft aggregation training`. The aggregation training method gives the best performance for multi-card properties but slightly degrades single-card prediction quality. | 🔵 Model Architecture | 2025-01-28 |
| Test the effect of adding a random noise vector on feature importance and prediction stability. | [#272](https://github.com/ccao-data/model-res-avm/issues/272) | Adding random noise did not affect global performance stats but did expose prediction instability. A small number of predictions flipped between ~$100K and ~$10M, tracing back to erroneous high-value sales that had passed sales validation. Correcting the underlying sales stabilized predictions. | 🟢 Training Process | 2025-01-22 |
| There may be data published by specific municipalities in the County — like Oak Park, Evanston, and Chicago — that we could use as features for PINs in those municipalities, leaving null for others. | [#376](https://github.com/ccao-data/model-res-avm/issues/376) | List of possible features provided in the issue, but no further action taken. | 🟣 Model Features | 2025-05-22 |
| Test a variance-based tree weighting strategy from the ML [literature](https://arxiv.org/abs/1611.07115) in the comps algorithm. | [#390](https://github.com/ccao-data/model-res-avm/issues/390) | Tested but did not meet requirements for adoption as the main algorithm. Report available in the `ei-issue-0050` output directory. | 🔴 Comps | 2025-10-06 |
| Investigate whether low sales volume distorts test set performance by using stratified sampling. Provide visualizations and metrics which can either compare different runs (enterprise-intelligence issue) or be used to analyze a single run (model-res-avm pull request) | [enterprise-intellignce #422](https://github.com/ccao-data/enterprise-intelligence/pull/442), [#516](https://github.com/ccao-data/model-res-avm/pull/516) | Three runs compared: the 2025 production baseline (most recent 10% of sales as holdout), a variant adding an additional +5% stratified sample, and a variant using a full 10% stratified sample with no recency holdout. No inconsistent errors attributable to time-tracking issues were found. Visualizations were not added to production reports; analysis preserved in `O:\CCAODATA\enterprise-intelligence\ei-issue-0438`. | 🟢 Training Process | 2026-07-09 |
| Balance test on whether sold properties in the 2025 training set are a representative sample of the full assessment universe. | [Enterprise Intelligence #453 (Private link, internal only)](https://github.com/ccao-data/enterprise-intelligence/issues/453) | Unweighted and IFW logistic regressions run on two attribute sources: training-level characteristics (as recorded at time of sale) and current characteristics. Two sources are used because some features drift over time; neighborhood-level median income can change independently of the parcel, and intentional changes like renovations can alter square footage after a sale. Training-level characteristics showed higher imbalance (AUC 0.61) than current characteristics (AUC 0.56), representing a low-to-moderate imbalance. Applying propensity score weighting reduced AUCs to 0.60 and 0.53, respectively. No downstream tests were conducted on model predictions. | 🟢 Training Process | 2026-07-20 |
