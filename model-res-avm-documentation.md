# Model-Res-AVM Issue Documentation

The Data Department has implemented and tested numerous improvements to the model-res-avm repository across modeling, infrastructure, reporting, and feature engineering. Not all tests and experiments have been incorporated into the final model. This document serves as a historical record of what has been explored, what was adopted, and what was set aside.

| Question / Goal | Issue | Findings / Output | Theme | Date |
| --- | --- | --- | --- | --- |
| Should `bonsai` (the official Tidymodels/parsnip shim for LightGBM) replace `Lightsnip`? | [#16](https://github.com/ccao-data/model-res-avm/issues/16) | `{bonsai}` is promising but not ready for adoption — it lacks advanced LightGBM hyperparameters and early stopping. Sticking with `Lightsnip`. | Modeling | 12-05-2023 |
| Add a workflow to run the model on PRs and `workflow_dispatch`. | [#23](https://github.com/ccao-data/model-res-avm/issues/23) | Workflow built that speeds up and standardizes model runs. | Infrastructure | 11-06-2023 |
| Consolidate all diagnostic reporting into a single Quarto document generated per model run, uploaded to S3 and linked in SNS completion notifications. | [#24](https://github.com/ccao-data/model-res-avm/issues/24) | Centralized `performance.qmd` file, producing run-specific outputs. | Reporting | 12-01-2023 |
| Test a City of Chicago–only model (using expanded city-specific data and only City triad sales) and compare performance to city-only ratios from county-wide models. | [#28](https://github.com/ccao-data/model-res-avm/issues/28) | Tested two configurations (Chicago-only sales; Chicago-only sales + large cross-validation). No change in performance in either case. | Modeling | 01-18-2024 |
| Prior CCAO residential models have used a single model that encompasses both single- and multi-family properties. We may achieve better accuracy for multi-family properties if we instead separate them out in a separate model. | [#29](https://github.com/ccao-data/model-res-avm/issues/29) | The result is essentially nil — splitting the models has no significant impact on overall performance statistics. This is somewhat expected, as the model already has access to plenty of information to differentiate between single- and multi-family properties. | Modeling | 12-05-2023 |
| We recently added Gini-based vertical equity statistics to the AssessR package. We should add `MKI` as one of the performance stats calculated during the `03-evaluate` stage. | [#30](https://github.com/ccao-data/model-res-avm/issues/30) | `MKI` added [here](https://github.com/ccao-data/model-res-avm/pull/72/changes#diff-e9699b64eb27e70f149338fe5977dcd81ce929bfb09f56a06b8bc1ec3f7f4e8d). | Reporting | 05-12-2023 |
| The `grid` argument of `tune_bayes()` can use a result from `tune_grid()`. We should use this to "seed" `tune_bayes()` with a manual or space-filling grid. | [#36](https://github.com/ccao-data/model-res-avm/issues/36) | The technique ended up being far slower than Bayesian optimization with early stopping. | Modeling | 12-25-2023 |
| Per recommendation from folks at posit::conf, test out the `Cubist` engine from the `rules` R package to see how it performs on our data. | [#37](https://github.com/ccao-data/model-res-avm/issues/37) | `Cubist` outperforms a linear model but not the current main model. Major drawbacks: single-threaded (very slow to train) and high memory usage (~250 GB), making a full grid search infeasible. | Modeling | 12-25-2023 |
| We may be able to cross-validate significantly faster using racing-based `tune` methods, which are available in the `finetune` R package. Test cross-validation techniques: standard grid search (with sub-model trick), tune racing, and simulated annealing. | [#38](https://github.com/ccao-data/model-res-avm/issues/38) | All techniques were slower than Bayesian optimization with early stopping. Sub-model fitting works but requires fitting on the maximum number of iterations. Grid racing is faster than standard grid search but has the same limitation. Simulated annealing is too slow. Bayesian optimization remains the best approach. | Modeling | 12-25-2023 |
| Add Median Absolute Percent Error (MeAPE) — the primary metric used by private AVMs like Zillow and Redfin — to the `03-evaluate` performance output. | [#40](https://github.com/ccao-data/model-res-avm/issues/40) | Metric added to reporting docs. | Reporting | 12-13-2023 |
| Revisit ensemble/stacked modeling using the `stacks` package (prior attempt with Tidymodels was inconclusive due to immaturity of the tooling at the time). | [#46](https://github.com/ccao-data/model-res-avm/issues/46) | The added complexity of a stacked model has not been worth it compared to a standard boosted tree model in prior testing. | Modeling | 12-06-2024 |
| Re-add a linear baseline model (using `glmnet`) for comparison against the tree-based main model. | [#48](https://github.com/ccao-data/model-res-avm/issues/48) | Linear model added using `recipes` for preprocessing. Outputs a `pred_card_initial_fmv_lin` column and a `stage="test_linear"` partition in the performance table. Reporting updated to include linear model in ratio curves, stats table, and predicted vs. actual plots. | Modeling | 01-04-2024 |
| Test categorical embeddings (via `embed.tidymodels.org`) as an alternative to LightGBM's native categorical handling. | [#49](https://github.com/ccao-data/model-res-avm/issues/49) | Embeddings performed worse than LightGBM's native handling. The `embed` package also has heavy dependencies. Embeddings removed entirely (including from the linear model). | Modeling | 01-27-2024 |
| Test a homeowner exemption model feature as a proxy for owner-occupied status. | [#50](https://github.com/ccao-data/model-res-avm/issues/50) | Feature added [here](https://github.com/ccao-data/data-architecture/pull/296/changes). | Model Features | 01-20-2024 |
| The Data Department has created or collected additional features since the 2023 model: new airport noise definition and distance to secondary roads. Update the model parameters, `ingest`, and code to use these. | [#110](https://github.com/ccao-data/model-res-avm/issues/110) | New features added to the [params.yaml file](https://github.com/ccao-data/model-res-avm/pull/148/changes). | Model Features | 01-04-2024 |
| Create a PIN-specific Quarto report with comparables, SHAP values, characteristics, and year-over-year value changes. | [#127](https://github.com/ccao-data/model-res-avm/issues/127) | Report created at `reports/pin/pin.qmd`. Includes link to assessor webpage, price/sale history, housing characteristics, SHAP waterfall chart, grouped SHAP values, and 3-year value change. | Reporting | 01-18-2024 |
| Test `Tidymodels` "1 standard deviation" rule selector to see if it improves generalization to a held-out validation set. | [#134](https://github.com/ccao-data/model-res-avm/issues/134) | The "best" model consistently generalizes better than the 1 SD selection. The 1 SD selector tends to prefer many shallow trees (3,000 iterations, <100 `num_leaves`) over fewer but deeper trees (1,500 iterations, <250 `num_leaves`). | Modeling | 02-07-2024 |
| Add per-property modeling features to help identify flips and rapidly changing areas: `sale count in prior N years`, `homeowner exemption indicator`, `length of occupancy`, `distance to university`, `distance to vacant lots`. | [#155](https://github.com/ccao-data/model-res-avm/issues/155) | New features added to the pipeline. | Model Features | 01-22-2024 |
| Add a price-strata or lagged-price feature to give the model a starting price point, enabling it to segment the market by value tier. | [#160](https://github.com/ccao-data/model-res-avm/issues/160) | Two problems identified: sold and unsold properties are treated differently due to the 3-year assessment cycle, and the lag price feature effectively leaks the outcome into the assessment set as a predictor. Feature scrapped. | Modeling | 02-03-2024 |
| Update cross-validation: revert to rolling-origin splitting, make it non-cumulative, and store per-fold predictions and residuals. | [#161](https://github.com/ccao-data/model-res-avm/issues/161) | New sliding-window CV function added with: switchable random (v-fold) vs. recency-based (sliding window) splitting via `validation_type`; non-cumulative folds; and overlapping fold support. | Modeling | 01-16-2024 |
| Replace LightGBM's native missingness handling with more targeted imputation using `recipes::step_impute_*` functions. | [#162](https://github.com/ccao-data/model-res-avm/issues/162) | No meaningful performance difference. Missingness is limited and LightGBM handles it well natively. Advanced strategies (bagging, KNN) were too slow to test even on large AWS instances. | Modeling | 01-29-2024 |
| Test log-transforming the outcome (sale price) and skewed input features (income, building sqft, etc.) to improve model performance, particularly at the low end. | [#163](https://github.com/ccao-data/model-res-avm/issues/163) | Results are inconclusive. Slightly improves COD and low-end accuracy but worsens overall RMSE and high-end accuracy. See commit `8a7ae37` and runs `2024-01-24-sagacious-liz` / `2023-12-26-kind-caroline`. | Modeling | 01-23-2024 |
| Test adding `class` code as a categorical predictor to better differentiate obscure property types. | [#165](https://github.com/ccao-data/model-res-avm/issues/165) | Including `class` code at the card level slightly improves overall model performance (run `2024-01-16-hungry-kyra`). Feature added to input data and parameter lists. | Model Features | 01-22-2024 |
| Test smaller geographic units as location features: census tracts + PUMA, census block groups + PUMA, and Sidwell ID broken out by block. | [#166](https://github.com/ccao-data/model-res-avm/issues/166) | No meaningful improvements across all tested combinations. Census tracts as numeric yielded a minor improvement (<1%). Sidwell ID by block performed worse than standard location predictors. | Model Features | 01-17-2024 |
| Create a per-run "challenge groups" report covering property types historically requiring analyst desk review: large lots, multi-card, multifamily, and top-decile properties. | [#189](https://github.com/ccao-data/model-res-avm/issues/189) | Report generated at `reports/challenge_groups/challenge_groups.qmd`. | Reporting | 04-18-2024 |
| The comps pipeline produces unexpected results for high-value cards, likely because tree weights are computed globally (based on training-set RMSE) rather than per observation. | [#193](https://github.com/ccao-data/model-res-avm/issues/193) | Updated the comps pipeline (`python/comps.py`) to use per-observation tree weights instead of global weights. | Comps | 02-03-2024 |
| Explore strategies for modeling multi-card properties, which are excluded from training because sale price cannot be attributed per card. | [#228](https://github.com/ccao-data/model-res-avm/issues/228) | Two best strategies identified: `single card summing sqft` and `multi-card sqft aggregation training`. The aggregation training method gives the best performance for multi-card properties but slightly degrades single-card prediction quality. | Modeling | 01-28-2025 |
| Create a reusable Quarto template for evaluating new model features, replacing the current ad-hoc process. | [#249](https://github.com/ccao-data/model-res-avm/issues/249) | Template created at `analyses/new-feature-template.qmd`. Test runs: new construction (`2024-07-03-charming-boni`), random column (`2024-07-13-great-eric`), no new columns (`2024-07-09-cool-takuya`), new construction + CV (`2024-07-07-mystifying-sam`). | Model Features | 09-05-2024 |
| Test the effect of adding a random noise vector on feature importance and prediction stability. | [#272](https://github.com/ccao-data/model-res-avm/issues/272) | Adding random noise did not affect global performance stats but did expose prediction instability. A small number of predictions flipped between ~$100K and ~$10M, tracing back to erroneous high-value sales that had passed sales validation. Correcting the underlying sales stabilized predictions. | Modeling | 01-22-2025 |
| Verify the pipeline is fully deterministic (identical inputs → identical outputs) by running it twice and diffing the `dvc.lock` files. | [#273](https://github.com/ccao-data/model-res-avm/issues/273) | Consistent seeds are used throughout. Two full local runs produced identical output file hashes. Pipeline confirmed deterministic. | Infrastructure | 12-20-2024 |
| Predictive models with a lot of heterogeneity in the training data often include features that measure the dispersion/variation of each predictor (e.g. standard deviation of year built, aggregated within 1,000 meters). | [#274](https://github.com/ccao-data/model-res-avm/issues/274) | Tests adding features based on the standard deviation of continuous variables had essentially no effect. | Model Features | 01-22-2025 |
| Most model improvements involve input data changes, but the build-and-run-model workflow only supports pulling training data from `DVC`, making it difficult to test new input data on `AWS Batch`. Fix this by adding a parameter to optionally run the ingest stage instead of pulling from `DVC`. | [#375](https://github.com/ccao-data/model-res-avm/issues/375) | Workflow updated to allow ingest to be run in the build-and-run-model workflow. | Infrastructure | 05-30-2025 |
| There may be data published by specific municipalities in the County — like Oak Park, Evanston, and Chicago — that we could use as features for PINs in those municipalities, leaving null for others. | [#376](https://github.com/ccao-data/model-res-avm/issues/376) | List of possible features provided in the issue, but no further action taken. | Model Features | 05-22-2025 |
| Test a variance-based tree weighting strategy from the ML [literature](https://arxiv.org/abs/1611.07115) in the comps algorithm. | [#390](https://github.com/ccao-data/model-res-avm/issues/390) | Tested but did not meet requirements for adoption as the main algorithm. Report available in the `ei-issue-0050` output directory. | Comps | 10-06-2025 |
| Consolidate prior one-off feature QC reports into a single reusable document analyzing input feature distributions, missingness, and year-over-year changes. | [#393](https://github.com/ccao-data/model-res-avm/issues/393) | Feature report created at `reports/model_features/model_features.qmd`, covering distributions, missingness, and changed values. | Reporting | 11-20-2025 |
| Remove the `210`/`295` class distinction from the complex ID construction algorithm, since fuzzy matching on year built is sufficient. | [#395](https://github.com/ccao-data/model-res-avm/issues/395) | Class removed from modeling in the ingest stage of the pipeline. | Modeling | 12-25-2025 |
| Formalize comps algorithm exploration by adding a configurable `comp.algorithm` parameter to `params.yaml`. | [#405](https://github.com/ccao-data/model-res-avm/issues/405) | Four algorithms now available in `python/comps.py`: `unweighted`, `weighted with error reduction`, `error reduction`, and `prediction variance`. | Comps | 02-26-2025 |
| Switch the model ingest stage to use the new `default.vw_pin_sale.is_outlier` column, which combines sales validation model results with analyst overrides. | [#414](https://github.com/ccao-data/model-res-avm/issues/414) | Modified sales validation outlier flag incorporated into the pipeline ingest stage. | Sales Val | 02-03-2026 |
| Remove legacy home improvement exemption (HIE) rolloff code from the assessment set `ingest` script, as HIEs are no longer being rolled off as of 2026. (Code must remain for the training set.) | [#439](https://github.com/ccao-data/model-res-avm/issues/439) | HIE value removed from the assessment set pipeline. | Modeling | 03-31-2026 |
| Move the model feature report off the server and into the GitHub Actions build-and-run-model workflow to reduce server load and speed up feedback cycles. | [#450](https://github.com/ccao-data/model-res-avm/issues/450) | Feature report added to the GitHub workflow with additional SHAP handling functionality. | Reporting | 03-17-2026 |
| Add training data predictions and evaluation metrics to the pipeline to enable overfitting detection and support future work (learning curves, ensembling, reweighting). | [#456](https://github.com/ccao-data/model-res-avm/issues/456) | Five new files generated per run and uploaded to S3/Athena: `training_card`, `performance_training`, `performance_training_linear`, `performance_quantile_training`, and `performance_quantile_training_linear`. | Reporting | 03-17-2026 |
| Add a parameter to subsample training and evaluation data, enabling faster iteration during model exploration and testing. | [#457](https://github.com/ccao-data/model-res-avm/issues/457) | New `ingest` logic added to support data subsampling. Reduces server iteration time by approximately 2 hours. | Modeling | 04-14-2026 |